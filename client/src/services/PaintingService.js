const URL = 'http://localhost:8989/api/paintings'

class PaintingService {
    // Get Paintings
    static getPaintings() {
        fetch(URL, {
            
        })
    }

    // Create Painting


    // Delete Painting
}